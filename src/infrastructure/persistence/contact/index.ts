/**
 * Contact Infrastructure - Persistence
 * 
 * Contains repository implementations for Contact persistence.
 * Implements interfaces defined in the domain layer.
 * 
 * @module infrastructure/persistence/contact
 */

// Export Contact repository implementations here
export * from './PostgresContactRepository';
// Example: export { ContactRepositoryImpl } from './ContactRepositoryImpl';
// Example: export { ContactQueryRepositoryImpl } from './ContactQueryRepositoryImpl';
