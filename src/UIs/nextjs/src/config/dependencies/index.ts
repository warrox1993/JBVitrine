/**
 * Dependency Injection Container
 * 
 * Central configuration for dependency injection.
 * Manages creation and wiring of all dependencies.
 * 
 * @module config/dependencies
 */

// Export DI container and registrations
// Example: export { container } from './container';
// Example: export { registerContactDependencies } from './contact.dependencies';
// Example: export { registerQuoteDependencies } from './quote.dependencies';
