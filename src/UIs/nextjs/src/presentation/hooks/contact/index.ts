/**
 * Contact Presentation - Hooks
 * 
 * Contains custom React hooks for Contact functionality.
 * Hooks connect UI components to application use cases.
 * 
 * @module presentation/hooks/contact
 */

export * from './useSubmitContact';
// Example: export { useSubmitContact } from './useSubmitContact';
// Example: export { useContactList } from './useContactList';
// Example: export { useContactDetails } from './useContactDetails';
