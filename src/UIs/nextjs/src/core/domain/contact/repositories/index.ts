/**
 * Contact Domain - Repository Interfaces
 * 
 * Contains repository interfaces (contracts) for data persistence.
 * Implementations are in the Infrastructure layer.
 * 
 * @module core/domain/contact/repositories
 */

// Export Contact repository interfaces here
export type { IContactRepository } from './IContactRepository';

// Example: export { IContactQueryRepository } from './IContactQueryRepository';
